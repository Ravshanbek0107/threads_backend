server:
  port: ${services.ports.thread}
spring:
  application:
    name: thread

  datasource:
    url: jdbc:postgresql://localhost:5432/thread_db
    username: postgres
    password: 1

  jpa:
    properties:
      hibernate:
        default_schema: thread

    hibernate:
      ddl-auto: update

    show-sql: true