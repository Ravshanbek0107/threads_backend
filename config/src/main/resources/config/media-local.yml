server:
  port: ${services.ports.media}
spring:
  application:
    name: media

  datasource:
    url: jdbc:postgresql://localhost:5432/thread_db
    username: postgres
    password: 1

  jpa:
    properties:
      hibernate:
        default_schema: media

    hibernate:
      ddl-auto: update

    show-sql: true

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

media:
  upload: C:\Users\99891\IdeaProjects\threads_backend\media\uploads